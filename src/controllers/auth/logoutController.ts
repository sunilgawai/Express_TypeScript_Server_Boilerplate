import { Request, Response, NextFunction, RequestHandler } from "express";

const logoutController: RequestHandler = async (req: Request, res: Response, next: NextFunction) => {
    // Your Code Goes Here....
    
}

export default logoutController;